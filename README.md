# Insitu Visualisation

A generic, lightweight library for 2D and basic 3D insitu visualisation.

The API expects to take in a piece of data in the form of

```cpp
template <typename T>
T* data;
```

## Docs


### Visualiser

The visualiser is the only script with an external dependency.
Ensure you have libsdl2 and libsdl2-dev

